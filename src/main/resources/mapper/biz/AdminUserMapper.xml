<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.keendo.biz.mapper.AdminUserMapper">
    <resultMap id="baseResultMap" type="com.keendo.biz.model.AdminUser">
        <result column="id" property="id" jdbcType="INTEGER" />
        <result column="username" property="username" jdbcType="VARCHAR" />
        <result column="password" property="password" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List" >
        `id` , `username` , `password` , `create_time` 
    </sql>

    <insert id="insert" parameterType="com.keendo.biz.model.AdminUser">
        insert into
            e_admin_user
                (`id` , `username` , `password` , `create_time` )
        values (#{id} ,#{username} ,#{password} ,#{createTime} )
    </insert>

    <select id="selectByUsername" parameterType="map" resultMap="baseResultMap" >
        SELECT <include refid="Base_Column_List"/>
        FROM e_admin_user
        WHERE username = #{username}
    </select>

    <select id="selectById" parameterType="map" resultMap="baseResultMap" >
        SELECT <include refid="Base_Column_List"/>
        FROM e_admin_user
        WHERE id = #{id}
    </select>
</mapper>
